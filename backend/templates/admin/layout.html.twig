<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}TalaTrivia - Administración{% endblock %}</title>
    
    <!-- Bootstrap 5 -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Font Awesome -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    
    <style>
        body {
            display: flex;
            min-height: 100vh;
        }
        .sidebar {
            min-width: 250px;
            max-width: 250px;
            background-color: #343a40;
            color: white;
            padding: 20px 0;
        }
        .sidebar .nav-link {
            color: rgba(255,255,255,.75);
            padding: 10px 20px;
        }
        .sidebar .nav-link:hover,
        .sidebar .nav-link.active {
            color: white;
            background-color: rgba(255,255,255,.1);
        }
        .main-content {
            flex: 1;
            padding: 20px;
            background-color: #f8f9fa;
        }
        .header {
            background-color: white;
            padding: 15px 20px;
            margin: -20px -20px 20px -20px;
            box-shadow: 0 2px 4px rgba(0,0,0,.1);
        }
    </style>
    
    {% block stylesheets %}{% endblock %}
</head>
<body>
    <div class="sidebar">
        <div class="px-3 mb-4">
            <h4>TalaTrivia</h4>
            <small class="text-muted">Panel de Administración</small>
        </div>
        <nav class="nav flex-column">
            {% if menuItems is defined %}
                {% for item in menuItems %}
                    <a class="nav-link" href="{{ path(item.route) }}">
                        <i class="fa {{ item.icon }}"></i> {{ item.label }}
                    </a>
                {% endfor %}
            {% else %}
                <a class="nav-link" href="{{ path('admin') }}">
                    <i class="fa fa-home"></i> Dashboard
                </a>
                <a class="nav-link" href="{{ path('admin_usuario_index') }}">
                    <i class="fa fa-users"></i> Usuarios
                </a>
                <a class="nav-link" href="{{ path('admin_trivia_index') }}">
                    <i class="fa fa-question-circle"></i> Trivias
                </a>
            {% endif %}
        </nav>
    </div>
    
    <div class="main-content">
        <div class="header">
            <h1>{% block page_title %}Administración{% endblock %}</h1>
        </div>
        
        <div class="content">
            {% block content %}{% endblock %}
        </div>
    </div>
    
    <!-- Bootstrap Bundle with Popper -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    {% block javascripts %}{% endblock %}
</body>
</html>
